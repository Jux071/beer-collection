<div class="well well-sm well-bgr"><h3>Countries 
<% if logged_in? %>
<%= link_to 'Add new country', new_country_path, class: 'btn btn-sm btn-primary' %>
	<% end %>
</h3>
		
  </div>
  
  <div class="panel panel-default">  
  	<div class="panel-heading"><strong>Country details</strong></div>  
  		<table class="table table-hover table-striped">	
				<thead>
					<tr>
						<th></th>
		    		<th>Name</th>
		    		<th><center>Total Beers</center></th>
		    		<% if logged_in? %>
		    <th>Actions</th>
		    <% end %>
					</tr>
				</thead>
				<tbody>
					<%= render @countries.order(name: :asc) %>
				</tbody>
			</table>
<div class="apple_pagination">  
		<div class="top"><%= page_entries_info @countries %></div>  
    <center><%= will_paginate @countries %></center>
  </div>
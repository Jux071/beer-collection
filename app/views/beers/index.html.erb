	<div class="well well-sm"><h3>Beers <%= link_to 'Add new beer', new_beer_path, class: 'btn btn-sm btn-primary' %></h3>
		
  </div>
  
  <div class="panel panel-default">  
  	<div class="panel-heading"><strong>Beer details</strong></div>  
  		<table class="table table-hover table-striped">	
				<thead>
					<tr>
		    		<th>Name</th>
		    		<th>Quantity</th>
		    		<th>Actions</th>
					</tr>
				</thead>
				<tbody>
					<%= render @beers.order(name: :asc) %>
				</tbody>
			</table>
